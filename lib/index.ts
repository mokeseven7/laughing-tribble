import * as cdk from '@aws-cdk/core';

export interface ServerlessFatfreeProps {
  // Define construct properties here
}

export class ServerlessFatfree extends cdk.Construct {

  constructor(scope: cdk.Construct, id: string, props: ServerlessFatfreeProps = {}) {
    super(scope, id);

    // Define construct contents here
  }
}
